package com.sirh.mqd.supplier.core;

/**
 * Classe de service pour l'export des journaux d'échanges
 *
 *
 */
// @Service(CoreConstantes.EXPORT_SERVICE)
public class ExportService {

	/*
	 * @Autowired
	 * 
	 * @Qualifier(PersistenceSupplierConstantes.ELASTICSEARCH_BC) private
	 * ElasticsearchBC elasticsearchBC;
	 * 
	 * public String getTextFile(final Date from, final Date to) {
	 * Page<WSGenericMetierEntity> result = elasticsearchBC.findEchange(from,
	 * to);
	 * 
	 * Iterator<WSGenericMetierEntity> echange = result.iterator();
	 * 
	 * StringBuffer reponse = new StringBuffer();
	 * 
	 * while (echange.hasNext()) { WSGenericMetierEntity WSGenericMetierEntity =
	 * echange.next(); reponse.append(WSGenericMetierEntity.getMessage() +
	 * "\n");
	 * 
	 * }
	 * 
	 * return reponse.toString(); }
	 */
}
